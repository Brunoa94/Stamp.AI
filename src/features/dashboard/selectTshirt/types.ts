// Sort options for t-shirt selection
export type SortOption = "price" | "name";

// Fit options for t-shirt filtering
export type FitOption = "regular" | "fitted" | "oversized";

// Material options for t-shirt filtering
export type MaterialOption = "cotton" | "polyester" | "blend";
