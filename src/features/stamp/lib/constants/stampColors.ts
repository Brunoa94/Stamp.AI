import type { SizeType } from "../types/stampTypes";

/**
 * Stamp Color Configuration
 *
 * Fabric colors for product customization
 */

export const STAMP_SIZES: SizeType[] = ["XS", "S", "M", "L", "XL", "XXL"];

export const DEFAULT_SIZE: SizeType = "M";
